const GraphQLObjectType = require('express-graphql');

//Rocket Type
const LaunchType = new GraphQLObjectType({
    name: 'Rocket',

})
//LaunchType
const LaunchType = new GraphQLObjectType({
    name: 'Launch',
    fields: ()=> ({

    })
})